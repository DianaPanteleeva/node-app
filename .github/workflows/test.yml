name: 🕵️‍♀️ DEBUG Files

on: [push]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check ALL files
        run: |
          echo "=== FULL FILE LIST ==="
          find . -type f | sort
          echo ""
          echo "=== DOCKERFILE INFO ==="
          find . -name "*ockerfil*" -type f -exec ls -la {} \;
          find . -name "*ockerfil*" -type f -exec echo "=== CONTENT ===" \; -exec cat {} \;
